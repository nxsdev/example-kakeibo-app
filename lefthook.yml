pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: npx oxlint {staged_files}
    format:
      glob: "*.{ts,tsx,js,jsx}"
      run: npx oxfmt --write {staged_files}
